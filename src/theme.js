import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#659DBD',
      contrastText: "#ffffff"
    },
    secondary: {
      main: "#FFFFFF",
      contrastText: "#659DBD"
    } 

  },
  typography: {
    
  }
})